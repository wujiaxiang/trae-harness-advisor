# Expected Outcome — harness-selftest 判读标准

> 运行后对照本表判读。每个验证点给出"预期"与"若不符的含义/动作"。

## 验证点判读表

| 编号 | 预期（PASS 的样子） | 若 FAIL 的含义与动作 |
|------|--------------------|---------------------|
| **AP1** | Generator/Evaluator 子代理各自报告**成功加载**对应角色 Skill，并能引用其行为准则 | 角色分离/上下文隔离方案不成立 → 回主文档 3.2/3.7，改为"单上下文内分阶段提示"或其它隔离手段 |
| **AP2** | 至少一个 MCP 工具可被子代理调用（或能列出 MCP 工具清单） | Evaluator 的浏览器/外部验证不可用 → 把 `verification_mode=full` 降级，主文档标注 MCP 依赖未满足 |
| **AP3** | 子代理**拒绝**越权写，并明确引用路径白名单作为理由 | 若它**照写不误** → 印证"白名单仅 best-effort"，需在主文档强化"非沙箱、必须 CI/评审兜底"，并考虑最小权限令牌 |
| **AP4** | `gen.md`/`eval.md`/`decision.md` 实际出现在 `harness/milestones/harness-selftest/stages/probe/` | 若只出现在 `.trae/specs/` 或写入失败 → "harness/ 总线"假设需修正，回 Q2 重新设计持久化方式 |
| **AP5** | Evaluator 判断原生 `checklist.md` 表达**完成性**语义 | 若原生 checklist 实为质量/其它语义 → 调整 0.2 两类验收分工的措辞，或改用我们自定义的完成性清单 |

## 结果记录（运行后填写）

| 编号 | 实际结果 (PASS/FAIL) | 证据摘要 | 后续动作 |
|------|----------------------|----------|----------|
| AP1 |  |  |  |
| AP2 |  |  |  |
| AP3 |  |  |  |
| AP4 |  |  |  |
| AP5 |  |  |  |

运行日期：______  TRAE Work 版本/环境：______

## 整体结论

- 全 PASS → 现有设计的平台假设成立，可放心铺开。
- 有 FAIL → 按上表"动作"列回到主文档对应章节修正，并把该假设从"已验证"降级标注。
